% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MDMediationFit.R
\name{MDMediationFit}
\alias{MDMediationFit}
\title{MDMediationFit: per-imputation mediation fits (S7)}
\usage{
MDMediationFit(
  per_imputation = list(),
  fits = list(),
  m = integer(0),
  engine = "glm",
  conf_int = FALSE,
  conf_level = 0.95,
  source = NULL
)
}
\arguments{
\item{per_imputation}{A list of named \link[medfit:MediationData]{medfit::MediationData} objects (length \code{m}).}

\item{fits}{A list of the raw backend fits (\code{lavaan}/\code{OpenMx}), one per imputation.}

\item{m}{Integer number of imputations.}

\item{engine}{medfit fitting engine used (e.g. \code{"glm"}).}

\item{conf_int}{Logical; whether output carries confidence intervals.}

\item{conf_level}{Numeric in (0, 1); confidence level.}

\item{source}{The originating \link{MDMediationData} (retained so MBCO can refit
constrained/unconstrained models against the imputed data).}
}
\value{
An \code{MDMediationFit} S7 object.
}
\description{
An S7 class holding the result of fitting a mediation model across all
imputations. Its defining feature is \code{per_imputation}: a list of \strong{named}
\link[medfit:MediationData]{medfit::MediationData} objects, one per imputation. This list is what the
MBCO-MI path consumes, because MBCO does not commute with Rubin's rules
(D4-stacked MBCO needs the per-imputation fits, not the pooled estimate).
}
\details{
It is the S7 successor of the S4 \link{SemResults} class.
}
\seealso{
\code{\link[=run]{run()}}, \code{\link[=per_imputation_list]{per_imputation_list()}}, \link{SemResults}
}
