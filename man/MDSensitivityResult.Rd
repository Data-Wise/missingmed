% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MDSensitivityResult.R
\name{MDSensitivityResult}
\alias{MDSensitivityResult}
\title{MDSensitivityResult: MNAR sensitivity curve (S7)}
\usage{
MDSensitivityResult(
  rungs = list(),
  grid = data.frame(),
  msp = numeric(),
  target = character(0),
  type = "mc",
  seed = integer(0),
  seed_source = "argument",
  method_target = NA_character_,
  source = NULL
)
}
\arguments{
\item{rungs}{List of inference results, one per row of \code{grid}.}

\item{grid}{Data frame of delta values; one column per target, one row per rung.}

\item{msp}{Numeric vector of realized marginal sensitivity parameters, one per
rung, computed on the first target (\code{target[1]}).}

\item{target}{Character; the shifted variable(s).}

\item{type}{Inference type used for each rung (\code{"mc"} or \code{"mbco"}).}

\item{seed}{Integer seed pinned across rungs.}

\item{seed_source}{\code{"mids"} if taken from the supplied \code{mids}, \code{"argument"} if
passed explicitly, \code{"default"} if neither was available.}

\item{method_target}{The \code{mice} imputation method(s) used for the target variable(s).}

\item{source}{The originating \link{MDMediationData}.}
}
\value{
An \code{MDSensitivityResult} S7 object.
}
\description{
An S7 class holding the result of \code{\link[=sensitivity_mnar]{sensitivity_mnar()}}: one inference result
per rung of a delta grid, plus the grid itself and the realized marginal
sensitivity parameters.
}
\section{The delta scale}{

\verb{@grid} holds \strong{conditional} sensitivity parameters (CSPs) -- the values the
user supplied. \verb{@msp} holds the \strong{marginal} sensitivity parameter actually
realized at each rung: the mean difference between imputed and observed values
of the target. They are not the same quantity and can differ substantially;
see \code{vignette("technical")} and \code{\link[=sensitivity_mnar]{sensitivity_mnar()}}.
}

\seealso{
\code{\link[=sensitivity_mnar]{sensitivity_mnar()}}
}
