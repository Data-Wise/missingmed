% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/accessors.R
\name{per_imputation_list}
\alias{per_imputation_list}
\title{Access the per-imputation mediation fits (for MBCO)}
\usage{
per_imputation_list(object, ...)
}
\arguments{
\item{object}{An \link{MDMediationFit} object.}
}
\value{
A list with components \code{per_imputation} (a length-\code{m} list of named
\link[medfit:MediationData]{medfit::MediationData}) and \code{m} (the number of imputations).
}
\description{
Returns the list of per-imputation \strong{named} \link[medfit:MediationData]{medfit::MediationData} objects
held in an \link{MDMediationFit}, together with the number of imputations \code{m}.
}
\details{
This accessor exists because \strong{MBCO does not commute with Rubin's rules}:
D4-stacked MBCO needs the per-imputation fits, not the pooled estimate. The
list it returns is the shape consumed by \code{\link[=infer]{infer()}}\code{(type = "mbco")} and by an
external \code{\link[RMediation:mbco]{RMediation::mbco()}} MI entry point (missingmed issue #2).
}
\seealso{
\code{\link[=run]{run()}}, \code{\link[=infer]{infer()}}
}
