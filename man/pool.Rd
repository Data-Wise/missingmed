% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pool.R
\name{pool}
\alias{pool}
\title{Pool per-imputation mediation fits with Rubin's rules}
\usage{
pool(object, ...)
}
\arguments{
\item{object}{An \link{MDMediationFit} object. Anything else (a \code{mice::mira},
say) is forwarded to \code{\link[mice:pool]{mice::pool()}}.}

\item{...}{Unused.}
}
\value{
An \link{MDMediationResult} object.
}
\description{
Applies Rubin's (1987) rules to the list of per-imputation \strong{named}
\link[medfit:MediationData]{medfit::MediationData} objects in an \link{MDMediationFit}, producing a single
pooled named \link[medfit:MediationData]{medfit::MediationData} (the \code{pooled} slot of the returned
\link{MDMediationResult}). Because the estimates and variance-covariance carry the
mediation path names (\code{a}, \code{b}, \code{c_prime}, ...), the pooled object is valid
input to \code{\link[RMediation:ci_mediation_data]{RMediation::ci_mediation_data()}} / \code{\link[RMediation:medci]{RMediation::medci()}}.
}
\details{
Pooling math (migrated from the S4 \code{pool_sem} / \code{pool_tidy} / \code{pool_cov}):
\deqn{\bar Q = \frac{1}{m}\sum_i Q_i, \quad \bar U = \frac{1}{m}\sum_i U_i,
  \quad B = \mathrm{cov}(Q_1, \ldots, Q_m), \quad T = \bar U + (1 + 1/m) B.}

It is the S7 successor of the S4 \code{\link[=pool_sem]{pool_sem()}} method.
}
\references{
Rubin, D. B. (1987). \emph{Multiple Imputation for Nonresponse in
Surveys}. Wiley.
}
\seealso{
\code{\link[=run]{run()}}, \code{\link[=infer]{infer()}}, \code{\link[=pool_sem]{pool_sem()}}
}
