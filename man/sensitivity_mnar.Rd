% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sensitivity_mnar.R
\name{sensitivity_mnar}
\alias{sensitivity_mnar}
\title{MNAR sensitivity analysis by delta-adjusted imputation}
\usage{
sensitivity_mnar(
  object,
  delta,
  target = NULL,
  type = c("mc", "mbco"),
  seed = NULL,
  level = NULL,
  n.mc = 1e+05,
  ...
)
}
\arguments{
\item{object}{An \link{MDMediationData} with \code{method = "mi"}.}

\item{delta}{Numeric vector (one rung per value, applied to \code{target}), or a
data frame (one rung per row, one column per target variable).}

\item{target}{Name of the variable to shift. Defaults to the mediator. Must
be \code{NULL} when \code{delta} is a data frame.}

\item{type}{Inference per rung: \code{"mc"} (default) or \code{"mbco"}.}

\item{seed}{Integer seed pinned across rungs. Defaults to the seed stored in
the \code{mids} object, or \code{20260822L} when that is \code{NA}.}

\item{level, n.mc}{Passed to \code{\link[=infer]{infer()}}.}

\item{...}{Passed to \code{\link[=run]{run()}}.}
}
\value{
An \link{MDSensitivityResult}.
}
\description{
Re-imputes the data across a grid of delta values and re-runs the mediation
pipeline at each rung, producing a \strong{sensitivity curve} for the indirect
effect. It is not an estimator: MAR versus MNAR is not testable from observed
data, so nothing here is identified. A rung answers "if the unobserved values
of \code{target} sit delta units away from what MAR imputation implies, the
indirect effect is X".
}
\section{Method}{

Delta-adjusted imputation in the pattern-mixture sense (van Buuren, \emph{FIMD}
§9.2; Leacy et al. 2017). For a continuous target the canonical procedure
imputes under MAR and then adds the constant to the imputed values (Hayati
Rezvan et al. 2018), which is what this function does via \code{mice}'s \code{post}
argument. Each rung re-imputes from the \code{mids} object's stored settings --
never from its recorded \code{call}, which does not resolve outside the function
that built it.
}

\section{The delta scale (read this before choosing a value)}{

\code{delta} is a \strong{conditional} sensitivity parameter (CSP): a difference
conditional on all remaining variables and their missingness indicators.
The quantity an analyst can actually reason about -- "non-respondents average
delta units higher" -- is a \strong{marginal} sensitivity parameter (MSP), and the
two are different numbers. Supplying an elicited MSP as if it were a CSP is
the standard failure mode of this method and can badly damage coverage
(Tompsett et al. 2018). This function therefore reports the \strong{realized MSP}
at every rung; compare it against what you meant.
}

\section{Limitations}{

\itemize{
\item Only \code{method = "mi"}. IPW has no imputations to shift.
\item Continuous targets only; see Details for categorical.
\item With \code{pmm} (mice's default), shifted values may fall outside the observed
range that \code{pmm} otherwise guarantees. A message is emitted once.
\item The curve assumes the supplied imputation model is compatible with the
mediation model; missingmed cannot verify this.
}
}

\seealso{
\code{\link[=infer]{infer()}}, \link{MDSensitivityResult}
}
